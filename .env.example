# ── Supabase (선택) ───────────────────────────────────────────────
# 파일럿 효과 측정용 진입 로그를 저장할 때만 필요. 없어도 서비스는 동작함.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# (클라이언트에서 직접 쓸 경우)
# NEXT_PUBLIC_SUPABASE_URL=
# SUPABASE_ANON_KEY=
